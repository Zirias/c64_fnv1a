MEMORY {
  MAIN:		start = $07ff, size = $8000;
  ZP:		start = $57, size = $10;
}

SEGMENTS {
  BHDR:		load = MAIN;
  MAINCODE:	load = MAIN;
  CODE:		load = MAIN;
  DATA:		load = MAIN;
  BSS:		load = MAIN, type = bss;
  ZEROPAGE:	load = ZP, type = zp;
}

